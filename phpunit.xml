<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!-- The suite outgrew PHP's default 128M: a full run died part way with
             "Allowed memory size exhausted" while every file passed on its own,
             which reads as a broken test rather than an exhausted runner. Pinned
             here so a full run behaves the same on a dev box and on CI, whatever
             the machine's php.ini says. -->
        <ini name="memory_limit" value="1G"/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <!-- Pin the n8n webhooks so tests that assert a send don't depend on a
             developer's local .env. Without these, CI copies .env.example (no
             N8N_* keys) and every Http::assertSent-style test fails because the
             code skips the send when the URL is unconfigured. -->
        <!-- Pinned for the same reason as the N8N_* URLs below: the code renders no
             embed when the Bunny library is unconfigured, so a test asserting on the
             player passes locally (dev .env has it) and fails on CI, which copies
             .env.example where it is unset. Second time this trap has been sprung. -->
        <env name="BUNNY_LIBRARY_ID" value="123456"/>
        <env name="N8N_STUDENT_WEBHOOK_URL" value="https://n8n.test/webhook/student"/>
        <env name="N8N_WEBHOOK_URL" value="https://n8n.test/webhook/general"/>
        <env name="N8N_ENROLLMENT_WEBHOOK" value="https://n8n.test/webhook/enrollment"/>
        <env name="N8N_INSTALLMENT_WEBHOOK" value="https://n8n.test/webhook/installment"/>
        <!-- Meta: the pixel partial renders nothing and every Conversions API /
             audience send is skipped when these are unset, so the same CI trap
             applies. META_TEST_EVENT_CODE is deliberately NOT pinned. -->
        <env name="META_PIXEL_ID" value="123456789012345"/>
        <env name="META_ACCESS_TOKEN" value="test-meta-token"/>
        <env name="META_AD_ACCOUNT_ID" value="1234567890"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
